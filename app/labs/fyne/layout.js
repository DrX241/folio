export const metadata = { 
  title: "FYNE — Lab 3 | Eddy MISSONI Idembi",
  description: "Plateforme immersive alimentée par IA — Hub de modules interactifs."
};

export default function FYNELayout({ children }) {
  return children;
}

