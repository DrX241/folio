export const metadata = { 
  title: "BI Studio — Lab 5 | Eddy MISSONI Idembi",
  description: "Business Intelligence & Décisionnel — Simulation interactive de création de dashboards BI."
};

export default function BIStudioLayout({ children }) {
  return children;
}

