export const metadata = { 
  title: "RAG Studio — Lab 1 | Eddy MISSONI Idembi",
  description: "Simulation pédagogique du pipeline RAG : ingestion, vectorisation, recherche, génération."
};

export default function RAGStudioLayout({ children }) {
  return children;
}

