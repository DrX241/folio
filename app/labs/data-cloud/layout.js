export const metadata = { 
  title: "DATA & CLOUD Lab — Lab 4 | Eddy MISSONI Idembi",
  description: "Industrialisation & Architecture — Schéma interactif de flux cloud."
};

export default function DataCloudLayout({ children }) {
  return children;
}

